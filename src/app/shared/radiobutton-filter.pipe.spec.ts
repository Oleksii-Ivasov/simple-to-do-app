import { RadiobuttonFilterPipe } from './radiobutton-filter.pipe';

describe('RadiobuttonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RadiobuttonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
